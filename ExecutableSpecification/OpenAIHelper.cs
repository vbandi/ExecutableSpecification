using System;

namespace ExecutableSpecification
{
    internal static partial class OpenAIHelper
    {
        // Uncomment this method and add your API key and other required options

        /*

        public static OpenAiOptions GetOpenAiOptions()
        {
            // --- Using OpenAI API ---
            //var options = new OpenAiOptions();
            //options.ApiKey = Environment.GetEnvironmentVariable("OpenAI_API_KEY");
            //return options;

            // --- Using Azure ---
            //var options = new OpenAiOptions();
            //options.ProviderType = ProviderType.Azure;
            //options.ApiKey = Environment.GetEnvironmentVariable("AZURE_OPENAI_API_KEY");  // <-- Make sure the environment variable is set or change this
            //options.ApiVersion = "2023-03-15-preview";
            //options.ResourceName = "YOUR RESOURCE NAME"; // <-- change this
            //options.DeploymentId = "YOUR DEPLOYMENT ID";  // <-- change this

            //return options;
        }

        */
    }
}
